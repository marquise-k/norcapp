import React from 'react';

const Searchbar = () => (
  <div className="App">
    <form className="search-form">
      <input
        className="search-bar"
        id="search=bar"
        type="text"
        placeholder="Search"
      />
    </form>
  </div>
);

export default Searchbar;
