import React from 'react';

const Searchbar = () => (
  <>
    <form className="search-form">
      <input
        className="search-bar"
        id="search=bar"
        type="text"
        placeholder="Search"
      />
    </form>
  </>
);

export default Searchbar;
